class ApiConfig {
  static const String baseUrl = 'https://orocampo-api.vercel.app';
  static const Duration timeout = Duration(seconds: 15);
  static const Duration syncInterval = Duration(minutes: 5);
}
